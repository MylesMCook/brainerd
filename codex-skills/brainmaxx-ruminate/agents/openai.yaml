interface:
  display_name: "Brainmaxx Ruminate"
  short_description: "Mine older Codex sessions for missed durable memory"
  default_prompt: "Use $brainmaxx-ruminate to review repo-scoped Codex session history, preview durable findings, and write only after explicit confirmation."
